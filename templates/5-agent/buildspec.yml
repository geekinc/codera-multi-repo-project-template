version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: $NODE_VERSION
  build:
    commands:
      - echo "Validating agent structure..."
      - test -f CLAUDE.md || (echo "Missing CLAUDE.md" && exit 1)
      - echo "Agent structure OK."
